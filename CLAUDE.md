# CLAUDE.md — Nowcast 개발 지시서

> 강원랜드 × SDU-COSS 바이브코딩 캠프 · 팀 딸깍
> **Nowcast** — 초보자도 딸깍 한 번으로 확인하는 카지노 혼잡 예보·AI 가이드 서비스

이 문서는 Claude Code가 이 프로젝트를 개발할 때 따라야 하는 기준 문서다.
작업 시작 전 반드시 전체를 읽고, 각 단계 완료 시마다 실행 확인 후 다음 단계로 진행한다.

---

## 0. 절대 규칙 (모든 작업에 우선)

1. **데이터에 없는 값을 화면에 만들어내지 않는다.** 예약 잔여석, 실시간 좌석 상태 등 개방 데이터에 존재하지 않는 정보는 어떤 형태로도 표시 금지. 미래 날짜의 혼잡 등급은 반드시 "예측" 라벨과 함께 표시한다.
2. **API 인증키·Anthropic 키는 코드에 하드코딩 금지.** 환경변수(`.env.local`, Vercel 환경변수)로만 관리한다. 저장소에는 `.env.example`(키 이름만)을 포함한다.
3. **모든 화면은 스냅샷 데이터만으로도 동작해야 한다.** 외부 API 호출 실패가 화면 오류로 이어지면 안 된다 (심사 안정성).
4. **표현 톤:** "게임을 더 하게 돕는" 표현 금지. 헛걸음 방지·대기 단축·계획적 방문 프레임 유지. 확률·승률·필승법 관련 내용 생성 금지 (챗봇 포함).
5. 커밋은 기능 단위로 자주. 저장소는 Public, 마감 1시간 전 코드 프리즈.

---

## 1. 제출 요구사항 (설계 제약)

- 배포: **Vercel** (Next.js). 배포 URL은 로그인·비밀번호 보호 없이 접속 가능해야 함
- 저장소: GitHub **Public** + `README.md` (프로젝트 소개 / 활용 데이터셋명 / 주요 기능 / 실행 방법)
- 대표 스크린샷 1장: 혼잡 예보 캘린더 화면 캡처를 `docs/screenshot.png`로 저장
- 모바일(휴대폰)에서 정상 표시 확인 — 본 앱은 모바일 우선 설계이므로 기본 충족
- 공공데이터가 배포 환경에서 정상 표시되어야 함 → 아래 하이브리드 데이터 전략으로 해결

## 2. 기술 스택

- **Next.js 14 (App Router) + TypeScript**
- **Tailwind CSS** (디자인 토큰은 4장 참조)
- **Recharts** (추이 차트) — 캘린더 히트맵은 직접 그리드로 구현
- 챗봇: **Anthropic API** (`claude-sonnet-4-6`) — 서버 라우트(`/api/chat`) 경유, 스트리밍 응답
- 공공데이터: 서버 라우트에서만 호출 (CORS·Mixed Content·키 노출 원천 차단)
- 상태관리: React 기본 훅만 사용 (외부 상태 라이브러리 금지 — 2일 범위 유지)

## 3. 폴더 구조

```
nowcast/
├── CLAUDE.md
├── README.md
├── .env.example              # DATA_GO_KR_API_KEY= / ANTHROPIC_API_KEY=
├── data/
│   ├── snapshot_entry.json   # 일자별 입장객 (전처리 완료본)
│   ├── snapshot_noshow.json  # 발행 대비 실입장/미입장
│   ├── games.json            # 게임현황 (게임명·테이블/기기 수·난이도·한줄설명)
│   └── game_guides.json      # 게임별 진행 가이드 요약 텍스트 (챗봇 근거)
├── scripts/
│   └── build_snapshot.ts     # 원본 CSV → snapshot JSON 전처리 스크립트
├── src/
│   ├── app/
│   │   ├── page.tsx          # 홈 = 혼잡 예보 캘린더
│   │   ├── games/page.tsx    # 게임 추천
│   │   ├── guide/page.tsx    # AI 챗봇
│   │   └── api/
│   │       ├── forecast/route.ts   # 스냅샷+API 병합 → 예보 JSON
│   │       └── chat/route.ts       # Anthropic 프록시
│   ├── components/           # AppFrame, TabBar, CalendarHeatmap, GameCard, ChatUI, ForecastBadge
│   └── lib/
│       ├── forecast.ts       # 혼잡 등급 계산 (5장 알고리즘)
│       ├── holidays.ts       # 한국 공휴일 목록 (2023~2026 하드코딩)
│       └── data.ts           # 스냅샷 로드 + API 폴백 로직
└── docs/screenshot.png
```

## 4. 디자인 시스템 (강원랜드 브랜드 무드 — 다크)

Tailwind config에 토큰으로 등록 후 임의 색상 사용 금지:

| 토큰 | 값 | 용도 |
|---|---|---|
| `bg-base` | `#14161C` | 앱 배경 (딥 차콜) |
| `bg-card` | `#1E212B` | 카드 서피스 |
| `gold` | `#C9A45C` | CTA·강조 숫자·활성 탭 (절제해서 사용) |
| `burgundy` | `#6E2B3C` | 헤더 그라데이션 보조 |
| `ok` | `#4CAF7D` | 혼잡 등급: 원활 |
| `mid` | `#F5C451` | 혼잡 등급: 보통 |
| `busy` | `#E2695E` | 혼잡 등급: 혼잡 |
| `text-main` | `#F2F3F5` / `text-sub` `#9CA3AF` | 본문/보조 텍스트 |

- 폰트: Pretendard (CDN). 강조 숫자는 semibold + 크게 (하이원 홈페이지 "블랙잭 62대" 스타일)
- 레이아웃: 모바일 앱 프레임 `max-w-[390px] mx-auto min-h-screen`, 하단 고정 탭바 3개 (예보 / 게임 / AI 가이드)
- 헤더: "Nowcast" 로고 + 골드 포인트, `데이터 기준일: YYYY-MM-DD` 표시
- 모든 예측 셀·카드에는 작게 `예측` 뱃지 표기

## 5. 데이터 레이어

### 5-1. 활용 데이터셋 (공공데이터포털 · 제공기관 "(주)강원랜드")

| 데이터 | 용도 | 방식 |
|---|---|---|
| 일자별 카지노 입장객 현황 (+ 내국인/외국인 분리본, SHIFT 컬럼) | 혼잡 예보 (핵심) | CSV 스냅샷 + API 일 1회 갱신 |
| ARS·모바일 예약 현황 | 예보 보조 변수 | CSV 스냅샷 |
| 카지노발행대비출입현황 (내국인) / 외국인 입장권 발행현황 | 노쇼율 분석 | CSV 스냅샷 |
| 카지노게임현황 | 게임 카드 (테이블·기기 수) | 정적 JSON |
| 카지노게임 진행 가이드 시리즈 (블랙잭·룰렛·바카라·카지노워·텍사스홀덤·쓰리카드포커·다이사이 등) | 챗봇 규칙 근거 | 포털 상세 설명 텍스트를 `game_guides.json`으로 정리 |
| (선택) 실시간 외국인 국가별 일일 카지노 입장현황 (JSON API) | 홈 "오늘 입장" 라이브 위젯 | API 직접 (서버 라우트) |

⚠️ **실제 컬럼명은 다운로드한 CSV 헤더를 그대로 따른다.** 이 문서의 컬럼명은 추정이므로, `build_snapshot.ts` 작성 시점에 실제 헤더를 확인하고 매핑 상수로 분리할 것.

### 5-2. 하이브리드 전략 (제출 안내문 3-4 대응)

1. 원본 CSV를 `scripts/build_snapshot.ts`로 전처리 → `data/*.json` 커밋 (모든 화면의 기본 소스)
2. `/api/forecast`가 요청 시 공공데이터 API에서 최신분을 가져와 스냅샷과 병합 (메모리 캐시 24h)
3. API 실패 시 조용히 스냅샷만 반환 — 화면에는 "데이터 기준일"만 갱신 여부로 드러남

### 5-3. 혼잡 등급 계산 (`lib/forecast.ts`)

```
입력: 과거 2~3년 일자별(가능하면 SHIFT별) 입장객
1) 각 (월 × 요일 [× SHIFT]) 조합의 평균 입장객 계산
2) 공휴일 가중: 공휴일 평균 ÷ 평일 평균 비율을 곱함 (holidays.ts 참조)
3) 미래 날짜 D의 예상치 = 해당 (월×요일[×SHIFT]) 평균 × 공휴일 가중
4) 전체 예상치 분포 기준 상위 30% = busy, 하위 30% = ok, 나머지 = mid
출력: { date, shift?, level: 'ok'|'mid'|'busy', expected: number, basis: string }
```
- `basis`에는 근거 문자열 저장 (예: "최근 3년 8월 토요일 평균 5,820명") → 상세 패널과 챗봇이 그대로 인용
- SHIFT 컬럼이 실데이터에 있으면 SHIFT 단위, 없으면 일 단위로 자동 강등

### 5-4. 노쇼율

`노쇼율 = 미입장 인원 ÷ 발행 인원` — 월별·성수기/비수기 평균을 계산해 게임 탭 "방문 플래너 팁" 카드의 근거 수치로 표시 (내국인 기준 메인).

## 6. 화면 명세 (구현 순서대로)

### 6-1. 홈 — 혼잡 예보 캘린더 (`/`)
- 월 네비게이션 + 7×5 그리드 히트맵 (ok/mid/busy 3색, 미래 칸에 `예측` 뱃지)
- 상단 "오늘의 예보" 카드: 오늘 등급 + 예상 입장객 + basis 문구
- 날짜 탭 → 하단 시트: SHIFT별 등급(가능 시), 과거 동요일 평균, "이 주의 추천 방문일"
- (선택) 실시간 외국인 입장 위젯: "오늘 외국인 입장 ○○명 (실시간)"

### 6-2. 게임 추천 (`/games`)
- `games.json` 기반 게임 카드 리스트: 게임명 / 난이도 뱃지(입문·쉬움·보통) / 테이블·기기 수 게이지 / 한 줄 설명
- 정렬: 좌석 규모(기기+테이블 수) 내림차순 = "초보자가 앉기 쉬운 순"
- 하단 "방문 플래너 팁" 카드: 노쇼율 수치 + "근거: 입장권 발행 대비 실입장 데이터"
- 카드 탭 → 해당 게임 규칙 요약 (game_guides.json) 모달

### 6-3. AI 가이드 챗봇 (`/guide`)
- 채팅 UI (말풍선, 스트리밍 타이핑, 추천 질문 칩 3개: "처음인데 뭐부터 해요?" / "이번 주말 붐비나요?" / "블랙잭 규칙 알려줘")
- `/api/chat` 시스템 프롬프트 구성:

```
너는 강원랜드 카지노 첫 방문자를 돕는 안내 도우미 "Nowcast AI"다.
[컨텍스트]
1. 이번 주 혼잡 예보: {forecast 요약 JSON}
2. 게임별 좌석 규모: {games.json 요약}
3. 게임 규칙 요약: {game_guides.json}
4. 이용 절차: 신분증 지참 → 입장권 구매 → 환전소 칩 교환 → 게임 (하이원 공식 초보자 안내 기준)
[규칙]
- 답변 근거가 위 컨텍스트에 있으면 마지막 줄에 "근거: ..." 표기
- 컨텍스트에 없는 내용은 모른다고 답한다 (지어내기 금지)
- 승률·배팅 전략·필승법 질문은 정중히 거절하고 건전한 이용을 안내한다
- 미성년자 관련 질문에는 출입 불가(만 19세 이상)를 안내한다
- 존댓말, 3~6문장, 처음 방문자 눈높이
```

## 7. 개발 순서 (마일스톤 — 각 단계 끝마다 `npm run build` + 배포 확인)

1. **M0 (사전)**: 보일러플레이트 + 디자인 토큰 + AppFrame/TabBar → Vercel 연결·환경변수 등록·첫 배포
2. **M1**: `build_snapshot.ts` (실제 CSV 헤더 확인 → 매핑) + `forecast.ts` + 단위 테스트 수준 검증 (콘솔 출력으로 등급 분포 확인)
3. **M2**: 홈 캘린더 화면 완성 (스냅샷만으로 동작 확인)
4. **M3**: 게임 탭 (games.json + 노쇼 팁 카드)
5. **M4**: 챗봇 (/api/chat + UI) — 추천 질문 3개 시나리오로 응답 품질 확인
6. **M5**: `/api/forecast` API 병합 + 폴백 테스트 (키 제거 상태로 배포해도 동작하는지)
7. **M6**: README.md, 스크린샷, 시크릿 창·휴대폰 접속 확인, 코드 프리즈

## 8. README.md 필수 목차

프로젝트 한 줄 소개 / 활용한 강원랜드 공공데이터셋명 (전체 나열) / 주요 기능 3가지 / 기술 스택 / 실행 방법 (`npm i && npm run dev`, 환경변수 안내) / 배포 URL / 팀 딸깍 소개

## 9. 시연 시나리오 (M6에서 리허설)

1. 홈 접속 → 오늘의 예보 확인 → 이번 주말 날짜 탭 → "혼잡(예측)" 확인
2. 챗봇에 "이번 주말에 처음 가보려는데 언제 가서 뭘 하면 되나요?" → 예보 근거 포함 답변
3. 게임 탭 → 슬롯머신 카드 탭 → 규칙 요약 → 방문 플래너 팁의 노쇼율 확인
